Mihomo proxy integration for OPNsense.

This package installs the mihomo binary, OPNsense GUI pages, configd
actions, rc.d service, default configuration, dashboard assets,
subscription helper, and OPNsense menu metadata.
